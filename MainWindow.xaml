<Window x:Class="FlashyTimer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FlashyTimer"
        mc:Ignorable="d"
        Title="Flashy Timer v0.1" Height="280" Width="640">
    <Window.Resources>
        <local:TimerViewModel x:Key="timerVM"/>
    </Window.Resources>
    <Grid Background="{Binding Background, Source={StaticResource timerVM}}">
        <TextBlock x:Name="textBlock" HorizontalAlignment="Center" TextWrapping="Wrap" Text="{Binding Text, Source={StaticResource timerVM}}" VerticalAlignment="Center" FontSize="72" FontFamily="Courier New" FontWeight="Bold" TextAlignment="Right" Background="{Binding BackgroundColor, Source={StaticResource timerVM}}"/>
        <StackPanel Width="120" VerticalAlignment="Bottom" HorizontalAlignment="Left" >
            <Button x:Name="buttonStart40m" Content="40m" FontSize="20" Margin="7" Background="#FF71FF55" BorderBrush="#FF3A7F2C" />
        </StackPanel>
        <StackPanel Width="120" VerticalAlignment="Bottom" HorizontalAlignment="Right" >
            <Button x:Name="buttonStop" Content="Stop" FontSize="20" Margin="7" Background="#FFFF7878" BorderBrush="#FF803A3A" />
        </StackPanel>
    </Grid>
</Window>
